name: 'Basic'

on:
  push:
    branches:
    - main

jobs:
  build-test:
    name: 'build Job'

    runs-on: ubuntu-latest

    steps:
    - name: Hello World
      shell: bash
      run: |
        echo "Hello World"

  dev:
    name: 'dev Job'
    needs:
    - build-test

    runs-on: ubuntu-latest

    steps:
    - name: Hello World
      shell: bash
      run: |
        echo "Hello World"

  test1:
    name: 'Test1 Job'
    needs:
    - dev

    runs-on: ubuntu-latest

    steps:
    - name: Hello World
      shell: bash
      run: |
        echo "Hello World"
  
  test2:
    name: 'Test2 Job'
    needs:
    - dev

    runs-on: ubuntu-latest

    steps:
    - name: Hello World
      shell: bash
      run: |
        echo "Hello World"

  prod:
    name: 'production Job'
    needs:
    - test1
    - test2

    runs-on: ubuntu-latest

    steps:
    - name: Hello World
      shell: bash
      run: |
        echo "Hello World"